# Minimal Management. The provider list is rendered by apply_management.sh,
# which also injects the Helm values into core.kcm.config.
# NOTE: envsubst also expands variables inside comments -- keep them out.
apiVersion: k0rdent.mirantis.com/v1beta1
kind: Management
metadata:
  name: kcm
spec:
  release: ${RELEASE_NAME}
  core:
    # No capi block: an empty component fails the schema (Component requires at
    # least one property since KCM 1d435a32, after 1.12.0-rc2), and KCM takes
    # the template from the Release when core.capi is absent.
    kcm:
      config: {}
  providers:
${MANAGEMENT_PROVIDERS}
