name: 03upg01_valid
group: Service upgrades
description: >-
  Issue #3, scenario 1: a deployed dependsOn chain where one service is then
  upgraded. Only that service may roll out; the one in front of it and the one
  behind it must both stay exactly as they were.

# cert-manager is in the middle so both directions are checked at once.
upgrade:
  services:
    - name: cert-manager
      # A minor bump, so the change is visible in the template and chart names.
      version: 1.21.1
  expect:
    rolledOut:
      - cert-manager
    untouched:
      - traefik
      - kserve-crd

services:
  - name: traefik
    chart: traefik
    version: 41.2.0
    repo: oci://ghcr.io/k0rdent/catalog/charts
    namespace: traefik
    waitForPods: traefik-

  - name: cert-manager
    chart: cert-manager
    version: 1.20.2
    repo: oci://ghcr.io/k0rdent/catalog/charts
    namespace: cert-manager
    dependsOn: traefik
    waitForPods: cert-manager-
    values: |
      cert-manager:
        crds:
          enabled: true

  - name: kserve-crd
    chart: kserve-crd
    version: v0.18.0
    repo: oci://ghcr.io/k0rdent/catalog/charts
    namespace: kserve
    dependsOn: cert-manager
